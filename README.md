This is a simulator for the RISC-V architecture. It essentially works by taking a file containing bytecode for assembly instructions as input, and then the program simulates how a RISC-V computer
hardware would execute those instructions (with memory, registers, etc.)
